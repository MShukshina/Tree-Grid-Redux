import { ActionReducerMap} from '@ngrx/store';

import { IAppState } from '../state/app.state';
import { treeReducers } from './tree-grid.reducer';


/*export const appReducers: ActionReducerMap <IAppState, any> = {
  tree: treeReducers
};*/
