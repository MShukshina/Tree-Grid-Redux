import {ENodeActions, GetUsers} from './node.actions';
import {ofType} from '@ngrx/effects';
import Expected = jasmine.Expected;
import {type} from 'os';

describe( 'nodes actions', () => {
  /*it ('should create an action get users', () => {
    const action = new GetUsers();
    expect(action).toEqual({type: 'Get Users'});
  });*/
});
